{% extends 'layout/admin.html.twig' %}

{% block title %}Room RoleRate Form{% endblock %}

{% block main %}
    <div class="container mt-5">
        <h1 class="mb-4">{{ roomRoleRate.id is not null ? 'Edit Room RoleRate' : 'Create Room RoleRate' }}</h1>

        {{ form_start(form, { 'attr': { 'class': 'needs-validation', 'novalidate': 'novalidate' } }) }}

        <div class="row">
            <div class="col-md-6">
                <div class="form-group mb-3">
                    {{ form_label(form.room, 'Room', { 'label_attr': { 'class': 'form-label' } }) }}
                    {{ form_widget(form.room, { 'attr': { 'class': 'form-select' } }) }}
                    {{ form_errors(form.room, { 'attr': { 'class': 'text-danger' } }) }}
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group mb-3">
                    {{ form_label(form.userRole, 'User Role', { 'label_attr': { 'class': 'form-label' } }) }}
                    {{ form_widget(form.userRole, { 'attr': { 'class': 'form-select' } }) }}
                    {{ form_errors(form.userRole, { 'attr': { 'class': 'text-danger' } }) }}
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="form-group mb-3">
                    {{ form_label(form.hourlyRate, 'Hourly Rate', { 'label_attr': { 'class': 'form-label' } }) }}
                    {{ form_widget(form.hourlyRate, { 'attr': { 'class': 'form-control', 'placeholder': 'Enter hourly rate' } }) }}
                    {{ form_errors(form.hourlyRate, { 'attr': { 'class': 'text-danger' } }) }}
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_admin_room_role_rate_list', {idRoom: roomRoleRate.room.id}) }}" class="btn btn-secondary">Back to List</a>
            <button type="submit" class="btn btn-light m-0">
                {{ roomRoleRate.id is not null ? 'Update Room RoleRate' : 'Create Room RoleRate' }}
            </button>
        </div>

        {{ form_end(form) }}
    </div>
{% endblock %}
